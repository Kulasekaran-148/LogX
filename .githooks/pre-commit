#!/bin/bash

echo "Running clang-format before commit..."

# Run format target
make format

# Check if formatting changed anything
CHANGED_FILES=$(git diff --name-only)

if [ -n "$CHANGED_FILES" ]; then
    echo "Formatting changes detected."
fi

echo "Pre-commit formatting complete."
exit 0  