[Unit]
Description=LogX Logging Daemon
After=network.target
Wants=network.target

[Service]
Type=simple
ExecStart=/usr/sbin/logxd
Restart=on-failure
RestartSec=2
User=root
Group=root

# Optional but recommended
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
